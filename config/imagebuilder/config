# Minimal package additions for HG680P original OpenWrt image.
# ImageBuilder keeps the default profile packages; these lines only add USB tethering kmods.

# USB network base
CONFIG_PACKAGE_kmod-usb-net=y

# Android USB tethering and generic USB Ethernet tethering
CONFIG_PACKAGE_kmod-usb-net-rndis=y
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y
CONFIG_PACKAGE_kmod-usb-net-cdc-eem=y

# iPhone/iPad USB tethering kernel driver
CONFIG_PACKAGE_kmod-usb-net-ipheth=y
